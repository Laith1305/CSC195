#include <iostream>
#include <array>
#include <vector>
#include <list>
#include <stack>
#include <map>
#include <unordered_map>
using namespace std;
int arrrrrr(){
	
	array<string, 7> week = { "Sunday", "Monday", "Teusday", "Wednesday", "Thursday", "Friday", "Saturday" };


	for (int i = 0; i < week.size(); i++) {
		cout << week[i] << endl;
		return 0;
	}
}